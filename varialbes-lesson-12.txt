5 примеров не наберется. В основном использование индексов идет от неуверенности в порядке использования элементов массива. Но java гарантирует последовательность и в упрощенной форме for и как недавно вычитал даже в stream
Поэтому в коде используется в основном короткая форма
for (var limitation : ownerLimitations) {
for (var path : paths) {
for (var template : templates) {
for (String value : values) {
Нашел одно место где бизнес-элемент путь разбивается по точке в массив String[] и итерируется по индексам. Но здесь нам нужны индексы начиная со второго, т.к. начало пути ссылается на привязанный к нему шаблон. Этот кусок не нужен
for (int i = 1; i < pathArr.length; i++) {pathBuilder.append(pathArr[i]).append(".")
Есть еще пример испозьвания индекса, но там проблема в том что используется стандартный валидатор Spring, и поэтому для формирования ошибки в автоматическом режиме необходимо использовать индексы элемента котоыре не валидны. Но это и логично - валидны не все, и нужны индексы.
